server:
  port: 9002
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://git.oschina.net/didispace/config-repo-demo/
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/